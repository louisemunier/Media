{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formulaire d'ajout d'albums</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('album_index') }}">Retourner à la liste</a>
        </li>
    </ul>
{% endblock %}
